const initialGlobalState = {
  roles: [],
  categories: [],
  theme: "",
  language: "",
};

export const globalReducer = (state, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
