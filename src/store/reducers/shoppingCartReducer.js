const initialShoppingCardState = {
  cart: [],
  payment: {},
  adress: {},
};

export const shoppingCartReducer = (state, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
